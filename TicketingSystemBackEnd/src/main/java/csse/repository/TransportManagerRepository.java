package csse.repository;

import csse.entity.TransportManager;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransportManagerRepository extends JpaRepository<TransportManager, Integer> {
}
